import React, { Component } from 'react';

// var Els = ({el, color}) => {<li style={{color : color}}>{el}</li>};
// var Aficher_el = (elms, color) => { return (<ul>{ elms.map((el, index) =>{<Els Key={index} el={el} color={color}/>})}</ul>)};
// const root = ReactDOM.createRoot(document.getElementById('root'));
// root.render(<Aficher_el elms= {elms} color='red'/>);

export default App;
